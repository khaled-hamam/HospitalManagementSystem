<Window x:Class="HospitalManagementSystem.Views.Home"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HospitalManagementSystem.Views"
        xmlns:viewmodels="clr-namespace:HospitalManagementSystem.ViewModels"
        xmlns:views="clr-namespace:HospitalManagementSystem.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Hospital Management System" Height="600" Width="800">
    <Window.Resources>
        <DataTemplate x:Name="PatientsView" DataType="{x:Type viewmodels:PatientsViewModel}">
            <views:PatientsView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Name="DepartmentsView" DataType="{x:Type viewmodels:DepartmentsViewModel}">
            <views:DepartmentsView DataContext="{Binding}"/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <DockPanel Name="LogoPanel" Background="{StaticResource AccentColor}" Grid.Row="0" Grid.Column="0" Grid.RowSpan="1">
            <TextBlock Text="Hospital" VerticalAlignment="Center" FontFamily="Arial" HorizontalAlignment="Center" FontSize="20" FontWeight="SemiBold" Foreground="White"/>
        </DockPanel>
        <DockPanel Name="SideMenu" Background="{StaticResource PrimaryColor}" Grid.Row="1" Grid.Column="0" Grid.RowSpan="9">
            <ListView x:Name="ListViewMenu" Margin="0 20 0 0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListViewItem x:Name="DepartmentButton" Style="{StaticResource NavButton}" Selected="DepartmentButton_Selected">
                    <TextBlock Text="Departments"/>
                </ListViewItem>
                <ListViewItem x:Name="PatientsButton" Style="{StaticResource NavButton}" Selected="PatientsButton_Selected">
                    <TextBlock Text="Patients"/>
                </ListViewItem>
                <ListViewItem Style="{StaticResource NavButton}">
                    <TextBlock Text="Appointments"/>
                </ListViewItem>
                <ListViewItem Style="{StaticResource NavButton}">
                    <TextBlock Text="Employees"/>
                </ListViewItem>
                <ListViewItem Style="{StaticResource NavButton}">
                    <TextBlock Text="Rooms"/>
                </ListViewItem>
            </ListView>
        </DockPanel>
        <ContentControl Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="4" Grid.RowSpan="10" Content="{Binding}"/>
    </Grid>
</Window>
